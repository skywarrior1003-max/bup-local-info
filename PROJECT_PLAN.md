# 프로젝트: BUP 현장 관리

한국 건설현장(소방·전기·설비) **하도급 업체**를 위한 모바일 웹앱.

## 왜 만드는가

하도급 현장의 자재·인력·원가는 지금도 종이 명세서, 카톡 사진, 엑셀로 흩어져 관리된다.
자재가 들어온 날 명세서는 현장 사무실에 쌓이고, 그 숫자가 원가에 반영되는 것은 한참 뒤다.
그래서 **현장소장이 "지금 이 현장이 남는 공사인지"를 그 자리에서 알 수 없다.**

이 제품은 그 간격을 없애는 것을 목표로 한다.
현장에서 명세서를 사진 한 장 찍으면 AI가 품목과 수량을 읽고, 검수만 누르면 그대로 입고 기록이 되고,
그 즉시 원가 현황에 반영된다.

## 목표 사용자

| 역할 | 하는 일 |
|---|---|
| 사업자 | 여러 현장의 원가를 한눈에 본다 |
| 현장대리인 / 현장소장 | 자재 입고를 검수하고 작업일보를 승인한다 |
| 공무 | 서류와 정산을 정리한다 |
| 작업자 | 출퇴근을 찍고 작업 사진을 올린다 |

## 핵심 데모 흐름

이 제품이 무엇인지 보여주는 것은 아래 한 줄이다.
**끊기지 않고 이어지는 것이 가장 중요하다.**

```
현장 선택 → 현장 홈 → 명세서 촬영 → 자재 입고 확인(AI가 읽은 값)
  → 자재 입고 검수 → 자재 입고 현황 → 원가 현황
```

마지막 **원가 현황**이 이 제품이 보여주려는 결과 화면이다.
앞의 여섯 화면은 전부 이 화면에 도달하기 위한 과정이다.

## 화면 구성

폭 390px 세로 모바일 기준. PC 브라우저에서는 가운데에 휴대폰 크기로 표시된다.
하단 탭바는 **홈 · 공정 · 출역 · 서류 · 설정** 5개다.

| 경로 | 화면 |
|---|---|
| `/sites` | 현장 선택 |
| `/sites/[id]` | 현장 홈 |
| `/sites/[id]/material` | 자재 입고 현황 |
| `/sites/[id]/material/capture` | 명세서 촬영 |
| `/sites/[id]/material/capture-warn` | 촬영 경고 |
| `/sites/[id]/material/confirm` | 자재 입고 확인 (AI가 읽은 값) |
| `/sites/[id]/material/inspect` | 자재 입고 검수 |
| `/sites/[id]/cost` | **원가 현황** |
| `/sites/[id]/photo/before` · `after` · `result` · `result-fail` | 작업 전/후 사진 |
| `/sites/[id]/report` · `report/approved` | 작업일보 |
| `/sites/[id]/attendance` · `worker` | 출역 · 작업자 |
| `/kiosk` · `checkin` · `checkout` · `fail` | 현장 출입 단말 |

### 출입 단말(KIOSK)이 다른 이유

현장 입구에 고정해 두는 태블릿 가로(1280x800) 화면이다. 앞의 모바일 화면들과 규칙이 다르다.

- 로그인·설정·메뉴 버튼이 없다. 하단 탭바도 없다.
- 스캔되면 출근/퇴근 화면으로 갔다가 3초 뒤 대기 화면으로 돌아온다.
- 화면 맨 위의 **「오프라인 — n건 대기 중」** 띠는 반드시 유지한다.
  건설 현장은 통신이 끊기는 곳이 많다. 끊겨도 기록은 남고 나중에 올라간다는 것이
  이 화면이 증명해야 할 핵심이다.

## 디자인 원칙 — Field-First Industrial System

건설 현장의 거친 환경에서의 시인성이 모든 판단의 기준이다.
장식적인 그림자·그라데이션·일러스트는 쓰지 않는다.

- **장갑을 낀 손** 기준으로 터치 영역을 잡는다. 기본 버튼 56px, 화면 하단 주요 액션 64px.
- **직사광선 아래** 기준으로 명도 대비를 잡는다. 딥 네이비(#022448 / #1E3A5F)가 주색이다.
- 깊이는 그림자가 아니라 **면 색과 1px 테두리**로 만든다.
- **숫자가 주인공이다.** 공정률·원가 같은 핵심 지표는 28px Bold로 가장 먼저 눈에 띈다.
- 아이콘은 절대 단독으로 쓰지 않는다. 항상 텍스트를 같이 둔다.
- 성공/주의/위험 색은 상태 표시에만 쓰고 장식으로는 쓰지 않는다.

토큰 정의는 [src/app/globals.css](src/app/globals.css)에 있다.

## 기술 스택

- Next.js 16 (App Router) + TypeScript + Tailwind CSS 4
- `output: 'export'` 정적 사이트 — 서버가 필요 없다
- Cloudflare Pages 호스팅 (`bup-local-info` 프로젝트, bupplatform.com)
- main 푸시 → GitHub Actions 빌드 → 자동 배포

## 현재 단계 — 시연용 목업

**지금 올라가 있는 것은 화면뿐이다.** 서버·DB·로그인은 붙어 있지 않고,
모든 숫자와 사진은 화면 안에 박아둔 샘플 데이터다.
샘플 현장은 `1` 동래 A현장, `2` 해운대 B현장, `3` 사상 C현장 세 개다.

이 단계의 목적은 **제품이 무엇인지 눌러보며 이해시키는 것**이다.

## 접속 정책

전체 공개가 아니다. [functions/_middleware.js](functions/_middleware.js)의 Cloudflare Pages Function이
`?key=<비밀키>`로 들어온 사람에게만 쿠키를 발급하고, 나머지에게는 "준비 중" 페이지를 보여준다.
검색엔진에도 노출되지 않는다.

비밀키는 이 저장소가 공개되어 있으므로 **코드가 아니라 Cloudflare 환경변수 `ACCESS_KEY`** 로 관리한다.

## 다음 단계 (제안)

아직 결정된 것이 아니라 검토 대상이다.

1. **플레이스홀더 이미지 교체** — 현재 사진은 전부 외부 임시 URL이라 만료 위험이 있다. 실제 현장 사진으로 교체한다.
2. **명세서 OCR 실연결** — 지금 "AI가 읽은 값"은 고정 샘플이다. 실제 인식 파이프라인을 붙인다. (PoC 데이터는 별도 `bup_poc`에서 검증 중)
3. **로그인과 역할 구분** — 사업자/현장소장/공무/작업자가 보는 화면이 달라야 한다.
4. **오프라인 큐** — KIOSK가 보여주는 "12건 대기 중"을 실제로 동작하게 만든다.
5. **데이터 저장소** — 현장·자재·출역·원가를 실제로 쌓는다. 이 시점에 정적 export를 벗어난다.

## 이전 버전

이 저장소는 원래 정부지원사업·지원금 정보 블로그였다.
해당 코드는 `legacy-blog` 브랜치와 `v0.1-blog` 태그에 보존되어 있다.
